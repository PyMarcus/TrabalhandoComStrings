import java.util.ArrayList;
import java.util.Collection;
import java.util.Collections;
import java.util.Scanner;


class Main{
    public static void main(String[] args){
        Scanner input = new Scanner(System.in);
       /****
        System.out.println("Informe um texto: ");
        Strings são um conjunto de caracteres que são inteiros baseados na tabela asc

        char s = 's'; // caractere s
        // valor inteiro de s, definido pelo SO, com base na tabela ascii:
        int valor_da_letra_s_em_inteiro = (int) s; // cast
        System.out.println("char s : " + s + " int s: " + valor_da_letra_s_em_inteiro);

        // s, 115, em binário
        int resto, finall, inicio, contador = 0;
        int novo_dividendo = 1;
        ArrayList<Integer> binario = new ArrayList<Integer>();
        while (novo_dividendo > 0){
            resto = valor_da_letra_s_em_inteiro % 2; // 0
            binario.add(resto);
            novo_dividendo = valor_da_letra_s_em_inteiro / 2;
            valor_da_letra_s_em_inteiro = novo_dividendo;
        }
        Collections.reverse(binario);
        System.out.println(binario + " = " + valor_da_letra_s_em_inteiro);

        GeneratePassword.gen();
        ****/



    }
}